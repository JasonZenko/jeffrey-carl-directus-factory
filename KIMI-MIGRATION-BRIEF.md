# Kimi K3 migration brief

You are the migration agent for the Jeffrey Carl clean factory benchmark. Work autonomously and make real repository changes.

Repository: /Users/jasonsibley/Code/jeffrey-carl-directus-factory

Stay on the configured kimi-code/k3 model.

Allowed inputs:

- FACTORY-CONTRACT.md
- AGENTS.md
- source-freeze/, the fresh complete capture of 78 live routes
- the public clean Directus API/schema at https://weomcms.foundryworks.ai if needed for understanding

Never read or copy the historical Jeffrey Carl three-page proof or Rocket City implementation.

Build the complete 78-route website in Astro, ready to consume native Directus records from the clean WEO master.

Implement:

1. Astro project with preserved legacy .asp paths and a noindex review surface.
2. Six page-family templates and the smallest correct reusable block modules matching the contract.
3. Deterministic source extraction into source-backed structured records with provenance for every block. Do not invent, rewrite, summarize, add generic CTAs, or collapse a whole page into one opaque HTML/JSON blob.
4. Directus import/provision scripts that create one site, all pages, native template relationships, ordered block relationships and managed assets in the existing clean master without changing global template definitions.
5. A build adapter that reads published Directus content with a server-only token, plus a frozen-data fallback that uses the identical typed contracts for disconnected Git review.
6. GitHub Actions for build/test and credentialed noindex Cloudflare preview deployment, with secrets referenced but never committed.
7. Tests for block contracts, route count, provenance, exact structural relationships, noindex controls and build output.
8. README and operator documentation.

Auditor integration:

- Render source-derived page content inside main[data-fidelity-root].
- Preserve each source article band as an ordered element carrying data-source-article="ArtID1", "ArtID2", etc.
- Header, navigation and footer stay outside data-fidelity-root.
- Do not create, edit, weaken or replace the independent auditor acceptance criteria in auditor/.
- You may read a failed receipt and repair your implementation, but you may not certify it.

Work through the whole estate, not a sample. Run tests. Return a concise record of files changed, tests run, route/block counts, unresolved blockers, elapsed time and Kimi usage if exposed.
