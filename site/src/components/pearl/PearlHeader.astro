---
import type { PearlTheme } from '../../lib/pearl/theme';

interface Props { theme: PearlTheme; }
const { theme } = Astro.props;
---

<header class="pearl-site-header">
  <div class="pearl-site-header__top pearl-wrap">
    <a class="pearl-brand" href="#top" aria-label={`${theme.brand_name} home`}>
      <strong>{theme.brand_name}</strong>
      {theme.brand_descriptor && <span>{theme.brand_descriptor}</span>}
    </a>
    <div class="pearl-site-header__details">
      {theme.appointment_label && theme.appointment_url && <a class="pearl-header-cta" href={theme.appointment_url}>{theme.appointment_label}</a>}
      {theme.address && <span>{theme.address}</span>}
      {theme.phone && <a href={`tel:${theme.phone.replace(/[^+\d]/g, '')}`}>{theme.phone}</a>}
    </div>
  </div>
  <nav aria-label="Pearl reference navigation">
    <div class="pearl-wrap">
      <a href="#top">home</a>
      <a href="#services">services</a>
      <a href="#about">about</a>
      <a href="#contact">contact</a>
    </div>
  </nav>
</header>

<style>
  .pearl-site-header { position: relative; z-index: 10; background: var(--pearl-surface); }
  .pearl-site-header__top { min-height: 118px; display: flex; align-items: center; justify-content: space-between; gap: 32px; }
  .pearl-brand { width: 190px; min-height: 138px; margin-bottom: -34px; display: grid; place-content: center; background: var(--pearl-secondary); color: var(--pearl-ink); text-align: center; text-decoration: none; }
  .pearl-brand strong { display: block; font-family: var(--pearl-font-heading); font-size: 3.15rem; font-weight: 400; line-height: .85; }
  .pearl-brand span { display: block; margin-top: 12px; font-size: .72rem; font-weight: 600; letter-spacing: .22em; text-transform: uppercase; }
  .pearl-site-header__details { display: flex; align-items: center; justify-content: flex-end; flex-wrap: wrap; gap: 10px 22px; color: var(--pearl-muted); font-size: .88rem; }
  .pearl-site-header__details a { color: inherit; }
  .pearl-header-cta { padding: 7px 13px; background: var(--pearl-primary); color: var(--pearl-ink) !important; font-size: .78rem; font-weight: 700; letter-spacing: .08em; text-decoration: none; text-transform: uppercase; }
  .pearl-site-header nav { background: var(--pearl-accent); }
  .pearl-site-header nav .pearl-wrap { min-height: 42px; display: flex; align-items: center; justify-content: flex-end; gap: 30px; }
  .pearl-site-header nav a { color: var(--pearl-ink); font-size: .86rem; font-weight: 600; text-decoration: none; }
  .pearl-site-header nav a:hover { text-decoration: underline; text-underline-offset: 4px; }

  @media (max-width: 720px) {
    .pearl-site-header__top { min-height: 104px; }
    .pearl-brand { width: 142px; min-height: 118px; }
    .pearl-brand strong { font-size: 2.45rem; }
    .pearl-site-header__details span { display: none; }
    .pearl-site-header nav .pearl-wrap { justify-content: flex-start; gap: 22px; overflow-x: auto; }
  }

  @media (max-width: 480px) {
    .pearl-site-header__details { display: none; }
  }
</style>
